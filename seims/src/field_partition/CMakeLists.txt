MESSAGE(STATUS "    Compiling FieldPartition...")
ADD_DEFINITIONS(-DMONGO_HAVE_STDINT -DUSE_MONGODB)
INCLUDE_DIRECTORIES(${PROJECT_BINARY_DIR})
LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
ADD_EXECUTABLE(fieldpartition util.cpp utils.cpp Raster.cpp Cell.cpp CellOrdering.cpp Field.cpp fieldTnode.cpp main.cpp)
SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR})
IF(WIN32)
	SET_PROPERTY(TARGET fieldpartition PROPERTY FOLDER "FieldPartition")
	TARGET_LINK_LIBRARIES(fieldpartition gdal_i.lib bson.lib mongoc.lib)
ELSE()
	TARGET_LINK_LIBRARIES(fieldpartition libgdal.so libmongoc.so libbson.so)
ENDIF()