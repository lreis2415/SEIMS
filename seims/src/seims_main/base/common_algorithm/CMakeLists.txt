MESSAGE(STATUS "        Compiling base_subdir: common_algorithm...")
IF(UNIX)
    IF(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_COMPILER_IS_ICC)
      SET(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -fPIC")
      SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -fPIC")
      geo_list_unique(CMAKE_C_FLAGS CMAKE_CXX_FLAGS)
    ENDIF()
ENDIF()
FILE(GLOB SRC_LIST *.cpp *.h)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(common_algorithm STATIC ${SRC_LIST})
TARGET_LINK_LIBRARIES(common_algorithm ${CCGLNAME})
INSTALL(TARGETS common_algorithm DESTINATION ${INSTALL_DIR})
IF (MSVC OR XCODE)
    SET_PROPERTY(TARGET common_algorithm PROPERTY FOLDER "base")
ENDIF ()
