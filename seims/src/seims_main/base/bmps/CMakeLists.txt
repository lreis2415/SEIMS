MESSAGE(STATUS "        Compiling base_subdir: bmps...")
IF(UNIX)
    IF(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_COMPILER_IS_ICC)
        SET(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -fPIC")
        SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -fPIC")
        geo_list_unique(CMAKE_C_FLAGS CMAKE_CXX_FLAGS)
    ENDIF()
ENDIF()
FILE(GLOB SRC_LIST *.cpp *.h)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(bmps STATIC ${SRC_LIST})
TARGET_LINK_LIBRARIES(bmps data util ${GDAL_LIBRARIES} ${BSON_LIBRARIES} ${MONGOC_LIBRARIES})

INSTALL(TARGETS bmps DESTINATION ${INSTALL_DIR})
IF (MSVC)
    SET_PROPERTY(TARGET bmps PROPERTY FOLDER "base")
ENDIF ()
