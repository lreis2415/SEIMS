MESSAGE(STATUS "        Compiling base_subdir: module_setting...")
IF(UNIX)
    IF(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_COMPILER_IS_ICC)
        SET(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -fPIC")
        SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -fPIC")
        geo_list_unique(CMAKE_C_FLAGS CMAKE_CXX_FLAGS)
    ENDIF()
ENDIF()
FILE(GLOB SRC_LIST *.cpp *.h)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(module_setting STATIC ${SRC_LIST})
TARGET_LINK_LIBRARIES(module_setting bmps data util)

INSTALL(TARGETS module_setting DESTINATION ${INSTALL_DIR})
IF (MSVC)
    SET_PROPERTY(TARGET module_setting PROPERTY FOLDER "base")
ENDIF ()
