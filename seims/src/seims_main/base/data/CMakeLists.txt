MESSAGE(STATUS "        Compiling base_subdir: data...")
IF (NOT MSVC)
    SET(CMAKE_CXX_FLAGS "-fPIC")
ENDIF()
AUX_SOURCE_DIRECTORY(. CUR_SRC_LIST)
SET(SRC_LIST ${CUR_SRC_LIST} ${RASTER_FILES} ${MONGO_FILES})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(data STATIC ${SRC_LIST})
TARGET_LINK_LIBRARIES(data util ${GDAL_LIBRARY} ${BSON_LIBRARIES} ${MONGOC_LIBRARIES})
INSTALL(TARGETS data DESTINATION ${INSTALL_DIR})
IF (MSVC)
    SET_PROPERTY(TARGET data PROPERTY FOLDER "base")
ENDIF ()
