MESSAGE(STATUS "        Compiling base_subdir: data...")
IF(UNIX)
    IF(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_COMPILER_IS_ICC)
        SET(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -fPIC")
        SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -fPIC")
        geo_list_unique(CMAKE_C_FLAGS CMAKE_CXX_FLAGS)
    ENDIF()
ENDIF()
FILE(GLOB CUR_SRC_LIST *.cpp *.h)
SET(SRC_LIST ${CUR_SRC_LIST} ${MONGO_FILES})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(data STATIC ${SRC_LIST})
TARGET_LINK_LIBRARIES(data util ${GDAL_LIBRARIES} ${BSON_LIBRARIES} ${MONGOC_LIBRARIES})

INSTALL(TARGETS data DESTINATION ${INSTALL_DIR})
IF (MSVC)
    SET_PROPERTY(TARGET data PROPERTY FOLDER "base")
ENDIF ()
