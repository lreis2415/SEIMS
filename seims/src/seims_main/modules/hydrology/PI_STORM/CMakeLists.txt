LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} Interception.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(PI_STORM SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET PI_STORM PROPERTY FOLDER "modules/hydrology")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(PI_STORM util data bmps module_setting)
ELSE ()
    TARGET_LINK_LIBRARIES(PI_STORM libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()