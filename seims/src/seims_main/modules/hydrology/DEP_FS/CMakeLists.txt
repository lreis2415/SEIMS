MESSAGE(STATUS "        Compiling hydrology_subdir: DEP_FS...")
FILE(GLOB SRC_LIST *.cpp *.h)
ADD_LIBRARY(DEP_FS SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
TARGET_LINK_LIBRARIES(DEP_FS module_setting)
INSTALL(TARGETS DEP_FS DESTINATION ${INSTALL_DIR})
IF (MSVC OR XCODE)
    SET_PROPERTY(TARGET DEP_FS PROPERTY FOLDER "modules/hydrology")
ENDIF ()
