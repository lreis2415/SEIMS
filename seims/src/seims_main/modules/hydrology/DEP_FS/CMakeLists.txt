LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} DepressionFS.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(DEP_FS SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET DEP_FS PROPERTY FOLDER "modules/hydrology")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(DEP_FS module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(DEP_FS libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()