LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} StormGreenAmpt.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(SUR_SGA SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET SUR_SGA PROPERTY FOLDER "modules/hydrology")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(SUR_SGA module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(SUR_SGA libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()