LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} Percolation.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(PERCO_DARCY SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET PERCO_DARCY PROPERTY FOLDER "modules/hydrology")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(PERCO_DARCY module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(PERCO_DARCY libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()