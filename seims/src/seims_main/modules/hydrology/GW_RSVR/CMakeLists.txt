LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} GWaterReservoir.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(GW_RSVR SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET GW_RSVR PROPERTY FOLDER "modules/hydrology")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(GW_RSVR module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(GW_RSVR libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()