MESSAGE(STATUS "        Compiling hydrology_subdir: HS_WB...")
SET(SRC_LIST ${MODULE_SRC} HS_WB.cpp api.cpp)
ADD_LIBRARY(HS_WB SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
TARGET_LINK_LIBRARIES(HS_WB util data bmps module_setting)
INSTALL(TARGETS HS_WB DESTINATION ${INSTALL_DIR})
IF (MSVC)
    SET_PROPERTY(TARGET HS_WB PROPERTY FOLDER "modules/hydrology")
ENDIF ()
