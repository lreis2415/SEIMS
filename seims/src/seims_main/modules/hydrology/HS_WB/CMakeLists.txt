LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} HS_WB.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(HS_WB SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET HS_WB PROPERTY FOLDER "modules/hydrology")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(HS_WB module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(HS_WB libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()