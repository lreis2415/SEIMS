MESSAGE(STATUS "        Compiling hydrology_subdir: IKW_IF...")
SET(SRC_LIST ${MODULE_SRC} InterFlow_IKW.cpp api.cpp)
ADD_LIBRARY(IKW_IF SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
TARGET_LINK_LIBRARIES(IKW_IF util data bmps module_setting)
INSTALL(TARGETS IKW_IF DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET IKW_IF PROPERTY FOLDER "modules/hydrology")
ENDIF ()
# IF (WIN32)
#     TARGET_LINK_LIBRARIES(IKW_IF util data bmps module_setting)
# ELSE ()
#     TARGET_LINK_LIBRARIES(IKW_IF libmodule_setting.a libbmps.a libdata.a libutil.a)
# ENDIF ()
