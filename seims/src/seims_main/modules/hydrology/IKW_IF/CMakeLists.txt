LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} InterFlow_IKW.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(IKW_IF SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET IKW_IF PROPERTY FOLDER "modules/hydrology")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(IKW_IF module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(IKW_IF libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()