MESSAGE(STATUS "        Compiling hydrology_subdir: IKW_CH...")
SET(SRC_LIST ${MODULE_SRC} IKW_CH.cpp api.cpp)
ADD_LIBRARY(IKW_CH SHARED ${SRC_LIST})
INSTALL(TARGETS IKW_CH DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET IKW_CH PROPERTY FOLDER "modules/hydrology")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(IKW_CH util data bmps module_setting)
ELSE ()
    TARGET_LINK_LIBRARIES(IKW_CH libmodule_setting.a libbmps.a libdata.a libutil.a)
ENDIF ()
