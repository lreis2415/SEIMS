LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} IKW_CH.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(IKW_CH SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET IKW_CH PROPERTY FOLDER "modules/hydrology")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(IKW_CH module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(IKW_CH libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()