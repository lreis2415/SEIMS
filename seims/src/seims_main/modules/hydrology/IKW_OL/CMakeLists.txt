LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} ImplicitKinematicWave.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(IKW_OL SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET IKW_OL PROPERTY FOLDER "modules/hydrology")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(IKW_OL module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(IKW_OL libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()