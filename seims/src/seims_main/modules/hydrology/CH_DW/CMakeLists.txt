MESSAGE(STATUS "        Compiling hydrology_subdir: DiffusiveWave...")
SET(SRC_LIST ${MODULE_SRC} DiffusiveWave.cpp api.cpp)
ADD_LIBRARY(CH_DW SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
TARGET_LINK_LIBRARIES(CH_DW util data bmps module_setting)
INSTALL(TARGETS CH_DW DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET CH_DW PROPERTY FOLDER "modules/hydrology")
ENDIF ()
# IF (WIN32)
#     TARGET_LINK_LIBRARIES(CH_DW util data bmps module_setting)
# ELSE ()
#     TARGET_LINK_LIBRARIES(CH_DW libmodule_setting.a libbmps.a libdata.a libutil.a)
# ENDIF ()
