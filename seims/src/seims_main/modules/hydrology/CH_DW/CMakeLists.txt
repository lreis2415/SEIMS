LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} DiffusiveWave.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(CH_DW SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET CH_DW PROPERTY FOLDER "modules/hydrology")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(CH_DW module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(CH_DW libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()