LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} Muskingum.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(CH_MSK SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET CH_MSK PROPERTY FOLDER "modules/hydrology")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(CH_MSK module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(CH_MSK libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()