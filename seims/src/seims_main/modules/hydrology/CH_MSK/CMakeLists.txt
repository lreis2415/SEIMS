MESSAGE(STATUS "        Compiling hydrology_subdir: CH_MSK...")
SET(SRC_LIST ${MODULE_SRC} Muskingum.cpp api.cpp)
ADD_LIBRARY(CH_MSK SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
INSTALL(TARGETS CH_MSK DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET CH_MSK PROPERTY FOLDER "modules/hydrology")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(CH_MSK util data bmps module_setting)
ELSE ()
    TARGET_LINK_LIBRARIES(CH_MSK libmodule_setting.a libbmps.a libdata.a libutil.a)
ENDIF ()
