LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} ExcessRunoff.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(SUR_EXCESS SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET SUR_EXCESS PROPERTY FOLDER "modules/hydrology")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(SUR_EXCESS module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(SUR_EXCESS libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()
