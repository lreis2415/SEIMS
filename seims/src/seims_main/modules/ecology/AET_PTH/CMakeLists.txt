MESSAGE(STATUS "        Compiling ecology_subdir: AET_PTH...")
SET(SRC_LIST ${MODULE_SRC} AET_PriestleyTaylorHargreaves.cpp api.cpp)
ADD_LIBRARY(AET_PTH SHARED ${SRC_LIST})
INSTALL(TARGETS AET_PTH DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET AET_PTH PROPERTY FOLDER "modules/ecology")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(AET_PTH util data module_setting)
ELSE ()
    TARGET_LINK_LIBRARIES(AET_PTH libmodule_setting.a libdata.a libutil.a)
ENDIF ()
