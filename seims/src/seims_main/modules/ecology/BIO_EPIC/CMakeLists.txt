MESSAGE(STATUS "        Compiling ecology_subdir: BIO_EPIC...")
SET(SRC_LIST ${MODULE_SRC} Biomass_EPIC.cpp api.cpp)
ADD_LIBRARY(BIO_EPIC SHARED ${SRC_LIST})
INSTALL(TARGETS BIO_EPIC DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET BIO_EPIC PROPERTY FOLDER "modules/ecology")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(BIO_EPIC util data bmps module_setting)
ELSE ()
    TARGET_LINK_LIBRARIES(BIO_EPIC libmodule_setting.a libbmps.a libdata.a libutil.a)
ENDIF ()
