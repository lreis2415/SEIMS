MESSAGE(STATUS "        Compiling erosion_subdir: MUSLE_I30...")
SET(SRC_LIST ${MODULE_SRC} MUSLE_I30.cpp api.cpp)
ADD_LIBRARY(MUSLE_I30 SHARED ${SRC_LIST})
INSTALL(TARGETS MUSLE_I30 DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET MUSLE_I30 PROPERTY FOLDER "modules/erosion")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(MUSLE_I30 util data bmps module_setting)
ELSE ()
    TARGET_LINK_LIBRARIES(MUSLE_I30 libmodule_setting.a libbmps.a libdata.a libutil.a)
ENDIF ()
