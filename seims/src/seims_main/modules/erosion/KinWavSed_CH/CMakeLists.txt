MESSAGE(STATUS "        Compiling erosion_subdir: KinWavSed_CH...")
SET(SRC_LIST ${MODULE_SRC} KinWavSed_CH.cpp api.cpp)
ADD_LIBRARY(KINWAVSED_CH SHARED ${SRC_LIST})
INSTALL(TARGETS KINWAVSED_CH DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET KINWAVSED_CH PROPERTY FOLDER "modules/erosion")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(KINWAVSED_CH util data bmps module_setting)
ELSE ()
    TARGET_LINK_LIBRARIES(KINWAVSED_CH libmodule_setting.a libbmps.a libdata.a libutil.a)
ENDIF ()
