MESSAGE(STATUS "        Compiling erosion_subdir: SEDR_SBAGNOLD...")
SET(SRC_LIST ${MODULE_SRC} SEDR_SBAGNOLD.cpp api.cpp)
ADD_LIBRARY(SEDR_SBAGNOLD SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
TARGET_LINK_LIBRARIES(SEDR_SBAGNOLD util data bmps module_setting)
INSTALL(TARGETS SEDR_SBAGNOLD DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET SEDR_SBAGNOLD PROPERTY FOLDER "modules/erosion")
ENDIF ()
# IF (WIN32)
#     TARGET_LINK_LIBRARIES(SEDR_SBAGNOLD util data bmps module_setting)
# ELSE ()
#     TARGET_LINK_LIBRARIES(SEDR_SBAGNOLD libmodule_setting.a libbmps.a libdata.a libutil.a)
# ENDIF ()
