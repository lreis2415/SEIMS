MESSAGE(STATUS "        Compiling erosion_subdir: SEDR_SBAGNOLD...")
FILE(GLOB SRC_LIST *.cpp *.h)
ADD_LIBRARY(SEDR_SBAGNOLD SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
TARGET_LINK_LIBRARIES(SEDR_SBAGNOLD module_setting)
INSTALL(TARGETS SEDR_SBAGNOLD DESTINATION ${INSTALL_DIR})
IF (MSVC OR XCODE)
    SET_PROPERTY(TARGET SEDR_SBAGNOLD PROPERTY FOLDER "modules/erosion")
ENDIF ()
