LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} SplashEro_Park.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(SPLASHERO_PARK SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET SPLASHERO_PARK PROPERTY FOLDER "modules/erosion")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(SPLASHERO_PARK module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(SPLASHERO_PARK libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()