LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} KinWavSed_OL.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(KINWAVSED_OL SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET KINWAVSED_OL PROPERTY FOLDER "modules/erosion")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(KINWAVSED_OL module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(KINWAVSED_OL libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()