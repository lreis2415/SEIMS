LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} template.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(template SHARED ${SRC_LIST})
IF (WIN32)
    TARGET_LINK_LIBRARIES(template module_setting bmps data util)
    IF (MSVC)
        SET_PROPERTY(TARGET template PROPERTY FOLDER "modules/test")
    ENDIF ()
ELSE ()
    TARGET_LINK_LIBRARIES(template libmodule_setting.so libbmps.so libdata.so libutil.so)
ENDIF ()
