LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} IO_test.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(IO_TEST SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET IO_TEST PROPERTY FOLDER "modules/test")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(IO_TEST module_setting bmps data util bson-1.0 mongoc-1.0)
ELSE ()
    TARGET_LINK_LIBRARIES(IO_TEST libmodule_setting.so libbmps.so libdata.so libutil.so libbson-1.0.so libmongoc-1.0.so)
ENDIF ()
