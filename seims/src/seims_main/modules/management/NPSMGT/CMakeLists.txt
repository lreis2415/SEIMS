MESSAGE(STATUS "        Compiling management_subdir: NPSMGT...")
SET(SRC_LIST ${MODULE_SRC} NonPointSource_Management.cpp api.cpp)
ADD_LIBRARY(NPSMGT SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
INSTALL(TARGETS NPSMGT DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET NPSMGT PROPERTY FOLDER "modules/management")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(NPSMGT util data bmps module_setting ${BSON_LIBRARIES} ${MONGOC_LIBRARIES})
ELSE ()
    TARGET_LINK_LIBRARIES(NPSMGT libmodule_setting.a libbmps.a libdata.a libutil.a ${BSON_LIBRARIES} ${MONGOC_LIBRARIES})
ENDIF ()
