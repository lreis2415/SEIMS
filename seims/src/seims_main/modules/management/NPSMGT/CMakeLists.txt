LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} NonPointSource_Management.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(NPSMGT SHARED ${SRC_LIST})
IF (WIN32)
    TARGET_LINK_LIBRARIES(NPSMGT bson-1.0 mongoc-1.0 module_setting bmps data util)
    IF (MSVC)
        SET_PROPERTY(TARGET NPSMGT PROPERTY FOLDER "modules/management")
    ENDIF ()
ELSE ()
    TARGET_LINK_LIBRARIES(NPSMGT libmodule_setting.so libbmps.so libdata.so libutil.so libbson-1.0.so libmongoc-1.0.so)
ENDIF()