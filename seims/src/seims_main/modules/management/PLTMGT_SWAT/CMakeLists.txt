MESSAGE(STATUS "        Compiling management_subdir: PLTMGT_SWAT...")
SET(SRC_LIST ${MODULE_SRC} managementOperation_SWAT.cpp api.cpp)
ADD_LIBRARY(PLTMGT_SWAT SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
TARGET_LINK_LIBRARIES(PLTMGT_SWAT util data bmps module_setting ${BSON_LIBRARIES} ${MONGOC_LIBRARIES})
INSTALL(TARGETS PLTMGT_SWAT DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET PLTMGT_SWAT PROPERTY FOLDER "modules/management")
ENDIF ()
# IF (WIN32)
#     TARGET_LINK_LIBRARIES(PLTMGT_SWAT util data bmps module_setting ${BSON_LIBRARIES} ${MONGOC_LIBRARIES})
# ELSE ()
#     TARGET_LINK_LIBRARIES(PLTMGT_SWAT libmodule_setting.a libbmps.a libdata.a libutil.a ${BSON_LIBRARIES} ${MONGOC_LIBRARIES})
# ENDIF ()
