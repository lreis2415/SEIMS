MESSAGE(STATUS "        Compiling management_subdir: PLTMGT_SWAT...")
FILE(GLOB SRC_LIST *.cpp *.h)
ADD_LIBRARY(PLTMGT_SWAT SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
TARGET_LINK_LIBRARIES(PLTMGT_SWAT bmps module_setting ${BSON_LIBRARIES} ${MONGOC_LIBRARIES})
INSTALL(TARGETS PLTMGT_SWAT DESTINATION ${INSTALL_DIR})
IF (MSVC OR XCODE)
    SET_PROPERTY(TARGET PLTMGT_SWAT PROPERTY FOLDER "modules/management")
ENDIF ()
