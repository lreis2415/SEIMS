LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} Nutrient_Transformation.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(NUTR_TF SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET NUTR_TF PROPERTY FOLDER "modules/nutrient")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(NUTR_TF module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(NUTR_TF libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()