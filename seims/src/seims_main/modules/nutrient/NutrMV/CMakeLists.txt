MESSAGE(STATUS "        Compiling nutrient_subdir: NUTRMV...")
FILE(GLOB SRC_LIST *.cpp *.h)
ADD_LIBRARY(NUTRMV SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
TARGET_LINK_LIBRARIES(NUTRMV module_setting common_algorithm)
INSTALL(TARGETS NUTRMV DESTINATION ${INSTALL_DIR})
IF (MSVC OR XCODE)
    SET_PROPERTY(TARGET NUTRMV PROPERTY FOLDER "modules/nutrient")
ENDIF ()
