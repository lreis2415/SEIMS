MESSAGE(STATUS "        Compiling nutrient_subdir: NUTRCH_QUAL2E...")
SET(SRC_LIST ${MODULE_SRC} NutrCH_QUAL2E.cpp api.cpp)
ADD_LIBRARY(NUTRCH_QUAL2E SHARED ${SRC_LIST})
INSTALL(TARGETS NUTRCH_QUAL2E DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET NUTRCH_QUAL2E PROPERTY FOLDER "modules/nutrient")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(NUTRCH_QUAL2E util data bmps module_setting)
ELSE ()
    TARGET_LINK_LIBRARIES(NUTRCH_QUAL2E libmodule_setting.a libbmps.a libdata.a libutil.a)
ENDIF ()
