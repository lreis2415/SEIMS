MESSAGE(STATUS "        Compiling nutrient_subdir: IUH_NUTR_OL...")
SET(SRC_LIST ${MODULE_SRC} IUH_NUTR_OL.cpp api.cpp)
ADD_LIBRARY(IUH_NUTR_OL SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
TARGET_LINK_LIBRARIES(IUH_NUTR_OL util data bmps module_setting)
INSTALL(TARGETS IUH_NUTR_OL DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET IUH_NUTR_OL PROPERTY FOLDER "modules/nutrient")
ENDIF ()
# IF (WIN32)
#     TARGET_LINK_LIBRARIES(IUH_NUTR_OL util data bmps module_setting)
# ELSE ()
#     TARGET_LINK_LIBRARIES(IUH_NUTR_OL libmodule_setting.a libbmps.a libdata.a libutil.a)
# ENDIF ()
