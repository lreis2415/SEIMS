MESSAGE(STATUS "        Compiling nutrient_subdir: NutrOL_IKW...")
SET(SRC_LIST ${MODULE_SRC} NutrOL_IKW.cpp api.cpp)
ADD_LIBRARY(NutrOL_IKW SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
INSTALL(TARGETS NutrOL_IKW DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET NutrOL_IKW PROPERTY FOLDER "modules/nutrient")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(NutrOL_IKW util data bmps module_setting)
ELSE ()
    TARGET_LINK_LIBRARIES(NutrOL_IKW libmodule_setting.a libbmps.a libdata.a libutil.a)
ENDIF ()
