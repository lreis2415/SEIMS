MESSAGE(STATUS "        Compiling nutrient_subdir: NutrCH_IKW...")
SET(SRC_LIST ${MODULE_SRC} NutrCH_IKW.cpp api.cpp)
ADD_LIBRARY(NutrCH_IKW SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
INSTALL(TARGETS NutrCH_IKW DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET NutrCH_IKW PROPERTY FOLDER "modules/nutrient")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(NutrCH_IKW util data bmps module_setting)
ELSE ()
    TARGET_LINK_LIBRARIES(NutrCH_IKW libmodule_setting.a libbmps.a libdata.a libutil.a)
ENDIF ()
