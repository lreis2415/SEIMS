MESSAGE(STATUS "        Compiling nutrient_subdir: NUTR_CH...")
SET(SRC_LIST ${MODULE_SRC} NUTR_CH.cpp api.cpp)
ADD_LIBRARY(NUTR_CH SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
INSTALL(TARGETS NUTR_CH DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET NUTR_CH PROPERTY FOLDER "modules/nutrient")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(NUTR_CH util data bmps module_setting)
ELSE ()
    TARGET_LINK_LIBRARIES(NUTR_CH libmodule_setting.a libbmps.a libdata.a libutil.a)
ENDIF ()
