LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} PETPenmanMonteith.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(PET_PM SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET PET_PM PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(PET_PM module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(PET_PM libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()