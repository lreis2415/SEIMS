LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} PETPriestleyTaylor.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(PET_PT SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET PET_PT PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(PET_PT module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(PET_PT libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()