MESSAGE(STATUS "        Compiling hydrology_longterm_subdir: SNO_DD...")
SET(SRC_LIST ${MODULE_SRC} SNO_DD.cpp api.cpp)
ADD_LIBRARY(SNO_DD SHARED ${SRC_LIST})
INSTALL(TARGETS SNO_DD DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET SNO_DD PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(SNO_DD util data bmps module_setting)
ELSE ()
    TARGET_LINK_LIBRARIES(SNO_DD libmodule_setting.a libbmps.a libdata.a libutil.a)
ENDIF ()
