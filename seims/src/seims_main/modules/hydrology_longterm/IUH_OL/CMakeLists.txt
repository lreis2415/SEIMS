MESSAGE(STATUS "        Compiling hydrology_longterm_subdir: IUH_OL...")
SET(SRC_LIST ${MODULE_SRC} IUH_OL.cpp api.cpp)
ADD_LIBRARY(IUH_OL SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
TARGET_LINK_LIBRARIES(IUH_OL util data bmps module_setting)
INSTALL(TARGETS IUH_OL DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET IUH_OL PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
# IF (WIN32)
#     TARGET_LINK_LIBRARIES(IUH_OL util data bmps module_setting)
# ELSE ()
#     TARGET_LINK_LIBRARIES(IUH_OL libmodule_setting.a libbmps.a libdata.a libutil.a)
# ENDIF ()
