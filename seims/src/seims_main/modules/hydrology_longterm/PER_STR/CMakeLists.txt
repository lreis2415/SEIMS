LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} PER_STR.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(PER_STR SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET PER_STR PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(PER_STR module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(PER_STR libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()