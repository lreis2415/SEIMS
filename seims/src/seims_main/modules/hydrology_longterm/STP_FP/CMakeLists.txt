LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} SoilTemperatureFINPL.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(STP_FP SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET STP_FP PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(STP_FP module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(STP_FP libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()