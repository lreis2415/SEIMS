LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} ExcessRunoff.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(SUR_ES SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET SUR_ES PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(SUR_ES module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(SUR_ES libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()
