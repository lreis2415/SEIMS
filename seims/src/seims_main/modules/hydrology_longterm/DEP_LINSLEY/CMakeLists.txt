MESSAGE(STATUS "        Compiling hydrology_longterm_subdir: DEP_LINSLEY...")
SET(SRC_LIST ${MODULE_SRC} DepressionFSDaily.cpp api.cpp)
ADD_LIBRARY(DEP_LINSLEY SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
INSTALL(TARGETS DEP_LINSLEY DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET DEP_LINSLEY PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(DEP_LINSLEY util data bmps module_setting)
ELSE ()
    TARGET_LINK_LIBRARIES(DEP_LINSLEY libmodule_setting.a libbmps.a libdata.a libutil.a)
ENDIF ()
