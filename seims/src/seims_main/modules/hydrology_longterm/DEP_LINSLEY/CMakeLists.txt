LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} DepressionFSDaily.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(DEP_LINSLEY SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET DEP_LINSLEY PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(DEP_LINSLEY module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(DEP_LINSLEY libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()