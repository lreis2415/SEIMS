MESSAGE(STATUS "        Compiling hydrology_longterm_subdir: PI_MSM...")
SET(SRC_LIST ${MODULE_SRC} clsPI_MSM.cpp api.cpp)
ADD_LIBRARY(PI_MSM SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
TARGET_LINK_LIBRARIES(PI_MSM util data bmps module_setting)
INSTALL(TARGETS PI_MSM DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET PI_MSM PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
# IF (WIN32)
#     TARGET_LINK_LIBRARIES(PI_MSM util data bmps module_setting)
# ELSE ()
#     TARGET_LINK_LIBRARIES(PI_MSM libmodule_setting.a libbmps.a libdata.a libutil.a)
# ENDIF ()
