LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} clsPI_MSM.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(PI_MSM SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET PI_MSM PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(PI_MSM module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(PI_MSM libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()