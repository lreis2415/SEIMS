LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} PETHargreaves.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(PET_H SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET PET_H PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(PET_H module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(PET_H libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()