MESSAGE(STATUS "        Compiling hydrology_longterm_subdir: PET_H...")
SET(SRC_LIST ${MODULE_SRC} PETHargreaves.cpp api.cpp)
ADD_LIBRARY(PET_H SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
INSTALL(TARGETS PET_H DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET PET_H PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(PET_H util data bmps module_setting)
ELSE ()
    TARGET_LINK_LIBRARIES(PET_H libmodule_setting.a libbmps.a libdata.a libutil.a)
ENDIF ()
