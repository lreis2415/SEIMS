MESSAGE(STATUS "        Compiling hydrology_longterm_subdir: SNO_SP...")
FILE(GLOB SRC_LIST ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
ADD_LIBRARY(SNO_SP SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
TARGET_LINK_LIBRARIES(SNO_SP util data bmps module_setting)
INSTALL(TARGETS SNO_SP DESTINATION ${INSTALL_DIR})
IF (MSVC OR XCODE)
    SET_PROPERTY(TARGET SNO_SP PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
