LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} SRD_MB.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(SRD_MB SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET SRD_MB PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(SRD_MB module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(SRD_MB libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()