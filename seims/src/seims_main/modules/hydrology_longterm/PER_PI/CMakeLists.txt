MESSAGE(STATUS "        Compiling hydrology_longterm_subdir: PER_PI...")
SET(SRC_LIST ${MODULE_SRC} PER_PI.cpp api.cpp)
ADD_LIBRARY(PER_PI SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
INSTALL(TARGETS PER_PI DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET PER_PI PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(PER_PI util data bmps module_setting)
ELSE ()
    TARGET_LINK_LIBRARIES(PER_PI libmodule_setting.a libbmps.a libdata.a libutil.a)
ENDIF ()
