MESSAGE(STATUS "        Compiling hydrology_longterm_subdir: IUH_IF...")
SET(SRC_LIST ${MODULE_SRC} IUH_IF.cpp api.cpp)
ADD_LIBRARY(IUH_IF SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
TARGET_LINK_LIBRARIES(IUH_IF util data bmps module_setting)
INSTALL(TARGETS IUH_IF DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET IUH_IF PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
IF (WIN32)
    #     TARGET_LINK_LIBRARIES(IUH_IF util data bmps module_setting)
    # ELSE ()
    #     TARGET_LINK_LIBRARIES(IUH_IF libmodule_setting.a libbmps.a libdata.a libutil.a)
    # ENDIF ()
