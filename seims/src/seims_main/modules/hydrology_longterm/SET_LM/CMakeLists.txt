LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} SET_LM.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(SET_LM SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET SET_LM PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(SET_LM module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(SET_LM libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()