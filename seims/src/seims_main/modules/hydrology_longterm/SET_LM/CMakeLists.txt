MESSAGE(STATUS "        Compiling hydrology_longterm_subdir: SET_LM...")
SET(SRC_LIST ${MODULE_SRC} SET_LM.cpp api.cpp)
ADD_LIBRARY(SET_LM SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
TARGET_LINK_LIBRARIES(SET_LM util data bmps module_setting)
INSTALL(TARGETS SET_LM DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET SET_LM PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
# IF (WIN32)
#     TARGET_LINK_LIBRARIES(SET_LM util data bmps module_setting)
# ELSE ()
#     TARGET_LINK_LIBRARIES(SET_LM libmodule_setting.a libbmps.a libdata.a libutil.a)
# ENDIF ()
