MESSAGE(STATUS "        Compiling hydrology_longterm_subdir: MUSK_CH...")
FILE(GLOB SRC_LIST ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
ADD_LIBRARY(MUSK_CH SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
TARGET_LINK_LIBRARIES(MUSK_CH util data bmps module_setting)
INSTALL(TARGETS MUSK_CH DESTINATION ${INSTALL_DIR})
IF (MSVC)
    SET_PROPERTY(TARGET MUSK_CH PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
