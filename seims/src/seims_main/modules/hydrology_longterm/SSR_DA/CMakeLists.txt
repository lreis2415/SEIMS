MESSAGE(STATUS "        Compiling hydrology_longterm_subdir: SSR_DA...")
SET(SRC_LIST ${MODULE_SRC} SSR_DA.cpp api.cpp)
ADD_LIBRARY(SSR_DA SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
TARGET_LINK_LIBRARIES(SSR_DA util data bmps module_setting)
INSTALL(TARGETS SSR_DA DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET SSR_DA PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
# IF (WIN32)
#     TARGET_LINK_LIBRARIES(SSR_DA util data bmps module_setting)
# ELSE ()
#     TARGET_LINK_LIBRARIES(SSR_DA libmodule_setting.a libbmps.a libdata.a libutil.a)
# ENDIF ()
