MESSAGE(STATUS "        Compiling hydrology_longterm_subdir: SSR_DA...")
FILE(GLOB SRC_LIST *.cpp *.h)
ADD_LIBRARY(SSR_DA SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
TARGET_LINK_LIBRARIES(SSR_DA module_setting)
INSTALL(TARGETS SSR_DA DESTINATION ${INSTALL_DIR})
IF (MSVC OR XCODE)
    SET_PROPERTY(TARGET SSR_DA PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
