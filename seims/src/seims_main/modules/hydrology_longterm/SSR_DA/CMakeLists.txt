LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} SSR_DA.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(SSR_DA SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET SSR_DA PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(SSR_DA module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(SSR_DA libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()