LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} IKW_REACH.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(IKW_REACH SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET IKW_REACH PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(IKW_REACH module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(IKW_REACH libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()