LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} ReservoirMethod.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(GWA_RE SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET GWA_RE PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(GWA_RE module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(GWA_RE libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()