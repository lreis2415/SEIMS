MESSAGE(STATUS "        Compiling climate_subdir: TSD_RD...")
SET(SRC_LIST ${MODULE_SRC} clsTSD_RD.cpp api.cpp)
LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
ADD_LIBRARY(TSD_RD SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
TARGET_LINK_LIBRARIES(TSD_RD module_setting util)
INSTALL(TARGETS TSD_RD DESTINATION ${INSTALL_DIR})
IF (MSVC)
    SET_PROPERTY(TARGET TSD_RD PROPERTY FOLDER "modules/climate")
ENDIF ()
