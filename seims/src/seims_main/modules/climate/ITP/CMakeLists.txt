MESSAGE(STATUS "        Compiling climate_subdir: ITP...")
SET(SRC_LIST ${MODULE_SRC} Interpolate.cpp api.cpp)
LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
ADD_LIBRARY(ITP SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
TARGET_LINK_LIBRARIES(ITP module_setting util)
INSTALL(TARGETS ITP DESTINATION ${INSTALL_DIR})
IF (MSVC)
    SET_PROPERTY(TARGET ITP PROPERTY FOLDER "modules/climate")
ENDIF ()
