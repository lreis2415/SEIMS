MESSAGE(STATUS "        Compiling climate_subdir: ITP...")
SET(MODNAME ITP)
PROJECT(SEIMS_MODULE_${MODNAME})
FILE(GLOB SRC_LIST *.cpp *.h)
LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
ADD_LIBRARY(${MODNAME} SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
TARGET_LINK_LIBRARIES(${MODNAME} module_setting ${CCGLNAME})
INSTALL(TARGETS ${MODNAME} DESTINATION ${INSTALL_DIR})
IF (MSVC OR XCODE)
    SET_PROPERTY(TARGET ${MODNAME} PROPERTY FOLDER "modules/climate")
ENDIF ()
