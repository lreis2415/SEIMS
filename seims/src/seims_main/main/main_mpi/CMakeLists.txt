MESSAGE(STATUS "        Compiling main_subdir: seims_mpi...")
SET(SRC_LIST ReadData.cpp main.cpp ManagementProcess.cpp CalculateProcess.cpp CombineRaster.cpp)
ADD_EXECUTABLE(seims_mpi ${SRC_LIST})
INSTALL(TARGETS seims_mpi DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET seims_mpi PROPERTY FOLDER "main")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(seims_mpi util data bmps module_setting main_mongodb ${GDAL_LIBRARY} ${BSON_LIBRARIES} ${MONGOC_LIBRARIES})
ELSE ()
    TARGET_LINK_LIBRARIES(seims_mpi libmain_mongodb.a libmodule_setting.a libdata.a libbmps.a libutil.a ${GDAL_LIBRARY} ${BSON_LIBRARIES} ${MONGOC_LIBRARIES})
ENDIF ()
