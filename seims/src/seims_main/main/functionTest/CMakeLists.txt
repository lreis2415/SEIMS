MESSAGE(STATUS "        Compiling main_subdir: functiontest...")
SET(SRC_LIST main.cpp)
ADD_EXECUTABLE(functionTest ${SRC_LIST})
SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR})
INSTALL(TARGETS functionTest DESTINATION bin)
IF (MSVC)
    SET_PROPERTY(TARGET c PROPERTY FOLDER "main")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(functionTest ${GDAL_LIBRARY} ${BSON_LIBRARIES} ${MONGOC_LIBRARIES} module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(functionTest ${GDAL_LIBRARY} ${BSON_LIBRARIES} ${MONGOC_LIBRARIES} libmodule_setting.a libdata.a libbmps.a libutil.a)
ENDIF ()
